name: power_manager
description: "A personal estimated energy tracking application."
publish_to: 'none'
version: 0.1.3+10011

environment:
  sdk: ^3.12.2

dependencies:
  crypto: ^3.0.7
  drift: 2.34.0
  drift_flutter: ^0.3.1
  file_selector: ^1.1.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  package_info_plus: ^10.2.1
  path_provider: ^2.1.6
  share_plus: ^13.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: 2.34.0
  build_runner: ^2.15.1
  test: ^1.31.0

flutter:
  uses-material-design: true
  shaders:
    - shaders/energy_orb.frag
