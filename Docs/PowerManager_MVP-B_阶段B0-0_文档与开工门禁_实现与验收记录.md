# PowerManager MVP-B 阶段 B0-0 实现与验收记录

## 1. 阶段结论

- 基线提交：`f0c90f2`。
- 状态：文档与开工门禁通过，允许进入 B0-1；本阶段没有修改生产学习开关或模型激活路径。
- 自动学习目标：明确为 MVP-B 核心，包含 baseline 与 activityImpact 两个独立参数族。

## 2. 已核对的交付物

- MVP-B PRD、规则配置、算法规则、技术方案和 Backlog 已形成 v3 基线；
- 总分阶段计划、B0-0 至 B3-2 独立 Spec、总体验收 Spec 和追踪矩阵已建立；
- `off / review / automatic`、版本化、未来生活日生效、通知、取消、撤回、暂停和全局单 pending
  的边界已在文档中统一；
- 真实数据才能决定的阈值、产品状态和生产门被移入 B1-1 / B3-0 / 总体验收，不用 fixture 猜测；
- 明确不追加与 MVP-B P0 无关的 MVP-A Stage 20 功能。

## 3. 验收与停止条件

- 文档职责、schema v1 → v5 顺序、工程完成与产品验证状态没有互相矛盾；
- 两个参数族默认模式为 `off`，自动学习总门、两个生产学习门和两个自动应用门均关闭；
- legacy 数据不会因迁移自动获得学习资格，用户主动反馈不会成为 sampledPrompt 生产证据；
- Markdown 结构、引用路径和术语检查通过，`git diff --check` 通过；
- 未命中 B0-0 停止条件，允许执行 B0-1。

本记录只证明文档和门禁成立，不证明真实用户学习效果；真实产品状态由独立产品验证报告维护。
