# PowerManager MVP-A 阶段 12：真实数据审计报告

## 0. 结论

- 审计日期：2026-08-09
- 数据文件：`powermanager-20260809-121121Z.json`
- SHA-256：`DFE36B25AE3238A1BB982D360823BFF0FA1D2EDFD25D682AC8CDEBC580803A6C`
- 导出版本：schema v1，App `0.1.0+2001`
- 数据一致性结论：**通过，发现 0 个计算或持久化一致性错误**
- 阶段 12 产品验证结论：**尚未通过，需先补足每日实际状态与主观记录**
- 自动学习结论：**不得开始**；当前带每日实际状态的有效日为 0，未达到 14 日门槛。

本报告只记录聚合统计和异常，不复制私人活动明细。

---

# 1. 数据概览

| 数据组 | 数量 |
|---|---:|
| 规则版本 | 1 |
| 晨间确认 | 7 |
| 活动记录 | 27 |
| 每日实际状态 | 0 |
| 随时校正 | 1 |
| 已结算生活日 | 8 |
| 有效生活日 | 6 |
| 提示回执 | 5 |
| 逻辑删除记录 | 0 |
| 检测到的编辑记录 | 0 |
| 检测到的补记样态 | 2 |

有效日由 5 个标准有效日和 1 个弱有效日组成；另有 2 个仅完成晨间确认、没有活动的
非有效结算日。数据覆盖 2026-07-26 至 2026-08-07，中间没有数据的自然日未生成空总结，
符合 MVP-A 的延迟持久化和候选日结算规则。

---

# 2. 一致性审计

以下检查全部通过：

1. JSON 可解析，根结构、schemaVersion 和七类业务数据齐全；
2. 所有活动、晨间确认和状态观测均归属于正确的 04:00 生活日；
3. 27 条活动的大类、子类、时长档位、规则版本和 theoreticalDelta 与内置规则表一致；
4. 活动按 `completedAt → createdAt → id` 稳定排序重放后，所有 appliedDelta 一致；
5. 正向恢复均受当日初始估计上限约束，没有超过恢复上限；
6. 8 个总结的初始估计、最终估计、消耗、恢复和四大类汇总全部可重算复现；
7. 紧邻前一生活日的负值短期修正正确，空日没有错误传播；
8. 标准有效日、弱有效日和非有效日判定一致；
9. 每日总结均在对应生活日结束后生成，延迟打开后的补结算顺序正确；
10. 提示回执和实际发生的中低、低、透支跨档完全对应，无漏记、重复或多余回执；
11. ID、每日唯一状态、晨间唯一性和提示业务键未发现冲突；
12. 删除状态和 deletedAt 约束一致；当前样本没有删除记录。

Android 历史退出信息和近期日志没有发现 Crash、ANR、`FATAL EXCEPTION` 或
`E/flutter`。审计期间产生的 `FORCE STOP` 属于人工检查操作，不计为应用故障。

---

# 3. 聚合使用信号

## 3.1 活动分布

| 大类 | 条数 | 总时长（分钟） | 理论净变化 | 应用净变化 |
|---|---:|---:|---:|---:|
| 学习 | 3 | 195 | -40 | -40 |
| 实践 | 11 | 660 | -186 | -186 |
| 恢复 | 6 | 315 | +15 | +5 |
| 休闲 | 7 | 585 | -111 | -111 |
| 合计 | 27 | 1755 | -322 | -332 |

恢复大类中的 6 条记录里，4 条规则为正恢复、1 条为 0、1 条为负消耗。理论恢复净值为
+15，但由于恢复上限，实际只应用 +5。该结果符合规则，但“恢复大类不一定恢复”可能造成
理解摩擦，后续应在选择时显示当前时长档位对应的估计变化，而不能只依赖大类名称。

## 3.2 有效日结果

| 指标 | 结果 |
|---|---:|
| 有效日 | 6 |
| 平均最终估计 | 42.3 |
| 平均最终值 / 初始值 | 43.6% |
| 最终值低于初始一半 | 3 日 |
| 最终值为负 | 1 日 |
| 总消耗 | 342 |
| 总恢复 | 10 |

前四个有效日的估计下降较明显，后两个有效日分别结束在初始值约 83% 和 75%。这可能
来自真实负荷，也可能说明默认规则偏重；因为没有每日实际状态，当前不能把二者区分，
更不能据此修改基准线或活动参数。

## 3.3 双轨反馈覆盖

- 每日实际状态：0 次；
- 随时校正：1 次，方向为“实际感觉比系统估计更低”；
- 具有每日实际状态的有效日：0 / 6；
- 达到基准线建议门槛：0 / 14。

这说明数据记录闭环已经工作，但“估计—实际”验证闭环尚未形成。当前最优先的问题不是
自动学习算法，而是让每日实际状态更容易被看到、理解并在正确时机完成。

---

# 4. 阶段 12 判定

| 验收项 | 结果 |
|---|---|
| 7 个自然日中至少 5 天真实主动使用 | 自动数据支持至少 5 个有效日；主动性仍需用户确认 |
| 至少 4 次每日实际状态 | **未通过：0 次** |
| 补记、编辑、删除、重算和结算无错误 | 重算与结算通过；2 条补记样态通过；编辑和删除没有真实样本 |
| 估计具有实际参考价值 | 无主观短日志，无法判定 |
| 无法分类或绕路不频繁 | 无主观短日志，无法判定 |
| 点击式记录稳定可用 | 27 条真实记录支持可用，但仍缺主观摩擦评分 |
| 数据错误 / 崩溃 | 0 / 0 |

因此阶段 12 不能判为“通过”，也没有证据表明产品或数据层失败。准确状态是：

> 数据层验证通过；产品验证材料不完整，优先修复每日实际状态的发现性，再继续积累。

---

# 5. 推荐下一阶段

## 5.1 P0：每日实际状态完成闭环

1. 当日已有活动但尚未确认实际状态时，在首页提供独立、低压的提示卡，而不是只放在
   底部工具带；
2. 下一生活日首次打开且昨日可补充时，把“补充昨日实际状态”放入昨日总结卡内部；
3. 保持先选择、后揭示估计，不用系统估计预先锚定选择；
4. 跳过不阻断记录，同一生活日不反复打扰；
5. 增加 Widget 测试覆盖显示、跳过、补充、已完成去重和跨日失效。

## 5.2 P1：备份与审计可用性

当前“导出并分享 JSON”依赖厂商分享面板，无法保证出现本地保存入口。建议拆为：

- “保存本机备份”；
- “分享 JSON”。

同时增加只读数据体检摘要，只展示有效日、实际状态覆盖、异常数量和最近导出时间，不新增
埋点或业务数据表。

## 5.3 暂不实施

- 自动基准线建议；
- personalFactor；
- 根据这 1 次随时校正修改活动规则；
- 降低点击式 `+` 的视觉权重。

这些项目仍缺少规定的真实样本或真机手势验收证据。
