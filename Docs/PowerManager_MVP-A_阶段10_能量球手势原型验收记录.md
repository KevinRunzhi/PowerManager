# PowerManager MVP-A 阶段 10：能量球手势原型验收记录

## 0. 当前状态

- 日期：2026-07-26
- 自动化测试：通过
- Pixel_7 模拟器：基础成功 / 取消路径通过
- Android 真机 ADB 触摸注入 20 次工程预验：20 / 20，0 次误记
- Android 真机人工手指 20 次可用性验收：待用户执行
- 阶段结论：**尚未转正，不降低 `+` 的视觉权重**

---

# 1. 已实现原型

- 120ms 长按启动；
- 大类、子类、时长三层径向选择；
- 34 / 88 / 148 logical px 三层半径阈值；
- 向球心回滑逐层清除选择；
- 只有 `ready` 状态松手才写入；
- 非 ready 松手、取消和 10 秒超时均不写入；
- 吸附使用 `selectionClick`，确认使用 `mediumImpact`；
- 确认后复用点击入口的 `ActivityMutator.create`；
- 写入后复用 5 秒撤销与提醒抑制时序；
- 失败时提示立即改用 `+`；
- 小屏边缘的显示节点会向安全区内收，命中角度不变。

方向映射：

| 层 | 上 | 右上 | 右下 | 下 | 左下 | 左上 |
|---|---|---|---|---|---|---|
| 大类 | 学习 | — | 实践 / 事务（右） | 休息恢复 | — | 娱乐消遣（左） |
| 子类 | 第 1 项 | 第 2 项 | 第 3 项 | 第 4 项 | 第 5 项 | 第 6 项 |
| 时长 | 15 | 30 | 45 | 60 | 90 | 120 |

---

# 2. 自动化证据

当前全量结果：

```text
dart format
通过

flutter analyze
通过，0 issue

flutter test
通过，111 tests

flutter build apk --debug --split-per-abi
通过
```

专项覆盖：

- 三层完整选择成功；
- 每层提前松手均返回 null；
- ready 后回滑依次退回时长、子类、大类；
- 取消状态不返回选择；
- 4 个大类、24 个子类、6 个时长均可达；
- 完整手势只调用一次共享创建用例；
- 不完整手势 0 次调用；
- 10 秒超时 0 次调用；
- 点击式记录回归继续通过。

---

# 3. 模拟器记录

设备：Pixel_7 / Android 14 / API 34。

| 任务 | 预期 | 结果 | 错误记录 |
|---|---|---|---|
| 上滑三层：学习 → 上课 → 15 分钟 | 新增一条记录 | 正确，记录数 1 → 2，出现 5 秒撤销 | 0 |
| 只选择大类后松手 | 取消 | 正确，记录数保持 2 | 0 |

无 `FATAL EXCEPTION`、`E/flutter` 或准备服务错误。

---

# 4. Android 真机工程预验

设备：

- 型号：V2359A
- 系统：Android 16
- ABI：arm64-v8a
- 分辨率 / 密度：1260 × 2800 / 560 dpi
- 输入方式：ADB `motionevent`，每次保持 180ms 后逐层移动

工程预验用于证明真机几何命中、状态转换和持久化安全，不代替真实手指对遮挡、
距离、震动和疲劳感的可用性判断。

执行前记录初始活动条数；每次结束后核对分类、子类、时长和新增条数。

| # | 任务 | 预期结果 | 实测 | 错误记录 |
|---:|---|---|---|---|
| 1 | 学习 → 上课 → 15 | 正确新增 | 通过 | 0 |
| 2 | 实践 → 实现 / 开发 → 30 | 正确新增 | 通过 | 0 |
| 3 | 恢复 → 午睡 / 小睡 → 45 | 正确新增 | 通过 | 0 |
| 4 | 娱乐 → 打游戏 → 60 | 正确新增 | 通过 | 0 |
| 5 | 学习 → 其他学习 → 120 | 正确新增 | 通过 | 0 |
| 6 | 实践 → 其他事务 → 90 | 正确新增 | 通过 | 0 |
| 7 | 恢复 → 其他恢复 → 60 | 正确新增 | 通过 | 0 |
| 8 | 娱乐 → 其他娱乐 → 45 | 正确新增 | 通过 | 0 |
| 9 | 学习 → 写作业 → 30 | 正确新增 | 通过 | 0 |
| 10 | 实践 → 项目推进 → 120 | 正确新增 | 通过 | 0 |
| 11 | 恢复 → 放空 / 调整 → 15 | 正确新增 | 通过 | 0 |
| 12 | 娱乐 → 看剧 / 观影 → 90 | 正确新增 | 通过 | 0 |
| 13 | 选到 15 后回滑，改为 120 | 只新增 120 | 通过 | 0 |
| 14 | 选到 120 后回滑，改为 15 | 只新增 15 | 通过 | 0 |
| 15 | 选到 45 后回滑，改为 60 | 只新增 60 | 通过 | 0 |
| 16 | 选子类后回滑，改选同类另一子类 | 只新增最终子类 | 通过 | 0 |
| 17 | 选大类后回滑，改选另一大类 | 只新增最终大类 | 通过 | 0 |
| 18 | 选到时长后回到球心再松手 | 取消，0 新增 | 通过 | 0 |
| 19 | 只选大类立即松手 | 取消，0 新增 | 通过 | 0 |
| 20 | 取消手势后立即用 `+` 完成记录 | 点击入口成功 | 通过 | 0 |

工程预验结果：20 / 20 正确，0 次错误记录；17 次手势成功写入、2 次取消
保持记录数不变、1 次点击兜底成功。

本应用 PID 日志无 `FATAL EXCEPTION` 或准备服务错误。Vivo / MediaTek 图形栈输出
了非致命的 gralloc 格式回退日志，界面和 20 次任务均正常；该项进入阶段 11
已知问题观察。

---

# 5. 人工手指可用性验收

仍需用户在同一真机上用真实手指重新执行上述 20 项，重点观察：

- 标签是否被手指遮挡；
- 34 / 88 / 148 logical px 是否容易跨层；
- 120ms 是否容易误触或等待感明显；
- 回滑是否能形成稳定肌肉记忆；
- 触觉是否过密；
- 连续使用是否疲劳。

人工验收通过条件：

通过条件：

- 20 次任务中至少 18 次结果正确；
- 0 次错误记录；
- 取消任务不落库；
- 手势失败后点击入口立即可用。

若未达到条件，原型保留为实验能力或延期，`+` 继续作为主入口，不阻塞其他
MVP-A 功能。
